{
  "_comment": "Reference tool roster for ambig-SWE SDK sessions. Captured live from ClaudeAgentOptions(tools={'type': 'preset', 'preset': 'claude_code'}, permission_mode='default', strict_mcp_config=True, mcp_servers={}, can_use_tool=<callback>) against claude-agent-sdk 0.2.128 on 2026-07-28, in a plain login shell. Capturing from a shell that is itself running under Claude Code is NOT valid: that environment sets CLAUDE_CODE_ENABLE_TASKS=0, which substitutes the legacy TodoWrite tool for the Task* tools below and yields a roster that no ordinary run reproduces. Personal MCP servers (Gmail/Calendar/Drive) are excluded via strict_mcp_config so the roster reflects a stock install. Every entry came from the live capture; no tool was added by hand, narrowed, or removed. Passing this list explicitly reproduces the preset roster exactly (31/31, verified).",
  "preset_source": "claude_code",
  "strict_mcp_config": true,
  "permission_mode": "default",
  "captured_at": "2026-07-28",
  "captured_in": "plain login shell (not nested under Claude Code)",
  "sdk_version": "0.2.128",
  "tools": [
    "AskUserQuestion",
    "Bash",
    "CronCreate",
    "CronDelete",
    "CronList",
    "DesignSync",
    "Edit",
    "EnterPlanMode",
    "EnterWorktree",
    "ExitPlanMode",
    "ExitWorktree",
    "Monitor",
    "NotebookEdit",
    "PushNotification",
    "Read",
    "RemoteTrigger",
    "ReportFindings",
    "ScheduleWakeup",
    "SendMessage",
    "Skill",
    "Task",
    "TaskCreate",
    "TaskGet",
    "TaskList",
    "TaskOutput",
    "TaskStop",
    "TaskUpdate",
    "ToolSearch",
    "WebFetch",
    "WebSearch",
    "Write"
  ]
}
